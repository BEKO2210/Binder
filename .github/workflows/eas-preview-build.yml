name: Binder EAS Preview Build
# Repository secret contract: EXPO_TOKEN.
# Manual-only: builds an installable internal APK for the Phase 7 device matrix.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: binder-eas-preview
  cancel-in-progress: false

jobs:
  android-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      CI: '1'
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Use Node.js 22
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '22.13.1'
          package-manager-cache: false

      - name: Require Expo authentication
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN repository secret is missing or empty." >&2
            exit 1
          fi

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Materialize production branding
        run: npm run brand:assets

      - name: Verify Phase 7 candidate
        run: |
          npm run verify:phase7-push
          npm run verify:brand
          npm run verify:phase6-design
          npm run verify:phase6-settings
          npm run verify:phase6-media
          npm run typecheck
          npm test

      - name: Build Android preview APK on EAS
        run: npx --yes eas-cli@16.19.3 build --platform android --profile preview --non-interactive
